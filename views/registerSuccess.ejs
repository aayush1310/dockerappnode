<!DOCTYPE html>
<html>

<head>
    <link rel="stylesheet" href='\stylesheets\style.css'/>
    <meta http-equiv="refresh" content="0;url=/login" />
</head>

<body>
        <%if (!userdetails) { %>
            <% include Template/header.ejs %>
        <% } %>
        
        <%if (userdetails) { %>
            <% include Template/header1.ejs %>
        <% } %>
        <h1>
            <%= title %>
        </h1>
        <div id="image">
            <center>
                <h3><%=userdetails.firstname%> you have Successfully Registered</h3><br><br>
                <h3>Redirecting in 2 Seconds.....</h3>
            </center>
        </div>
</body>

</html>